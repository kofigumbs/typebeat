{
  "name": "typebeat",
  "description": "Sampler, synth, and sequencer designed for the QWERTY keyboard.",
  "dependencies": {
    "firacode": "^5.2.0",
    "solid-js": "^1.2.1",
    "tare": "github:kofigumbs/tare"
  },
  "devDependencies": {
    "babel-preset-solid": "^1.2.1",
    "chokidar-cli": "^3.0.0",
    "toml": "^3.0.0",
    "vite": "^2.5.8",
    "vite-plugin-solid": "^2.1.1"
  },
  "scripts": {
    "start": "mkdir -p target/debug/samples; cp samples/* target/debug/samples; (vite ui --clearScreen false &); chokidar 'src/**/*.rs' build.rs --command 'pkill typebeat; cargo run' --initial; pkill node",
    "faust": "ls src/effects/*.dsp | xargs -I{} bash -c 'faust -lang rust -cn `basename -s .dsp {}` -o {}.rs {}'"
  },
  "workspaces": []
}
