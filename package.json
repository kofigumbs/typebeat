{
  "name": "typebeat",
  "description": "Play with music, quickly. Sampler + synth + sequencer. Designed for the QWERTY keyboard.",
  "dependencies": {
    "firacode": "^5.2.0",
    "solid-js": "^1.2.1",
    "tare": "github:kofigumbs/tare"
  },
  "devDependencies": {
    "babel-preset-solid": "^1.2.1",
    "vite": "^2.5.8",
    "vite-plugin-solid": "^2.1.1"
  },
  "scripts": {
    "start": "npm install; mkdir -p target/debug; ln -fs $PWD/samples target/debug/samples; (vite www --clearScreen false &); cargo watch --watch src --watch build.rs --exec run; pkill node"
  },
  "workspaces": []
}
